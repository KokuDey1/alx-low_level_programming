#include "main.h"

/**
 * _isdigit - searches for a digit (between 0 - 9)
 * @c: int to be searched
 * Return: 1 if c is a digit, 0 if otherwise
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
